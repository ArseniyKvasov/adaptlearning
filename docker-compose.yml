services:
  adaptive-learning-prototype:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: adaptive-learning-prototype
    restart: unless-stopped
    ports:
      - "8090:80"
